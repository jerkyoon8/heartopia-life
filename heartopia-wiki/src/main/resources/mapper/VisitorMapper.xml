<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heartopia.wiki.mapper.VisitorMapper">

    <resultMap id="visitorSummaryResult" type="com.heartopia.wiki.dto.VisitorSummary">
        <result property="todayVisitors" column="today_visitors"/>
        <result property="weeklyVisitors" column="weekly_visitors"/>
    </resultMap>

    <!-- Increment today's count -->
    <update id="incrementTodayCount">
        INSERT INTO visitor_stats (visit_date, visit_count)
        VALUES (CURDATE(), 1)
        ON DUPLICATE KEY UPDATE visit_count = visit_count + 1
    </update>

    <!-- Get today's and the last 7 days' counts in one DB round trip -->
    <select id="getVisitorSummary" resultMap="visitorSummaryResult">
        SELECT
            COALESCE(SUM(CASE WHEN visit_date = CURDATE() THEN visit_count ELSE 0 END), 0) AS today_visitors,
            COALESCE(SUM(visit_count), 0) AS weekly_visitors
        FROM visitor_stats
        WHERE visit_date >= DATE_SUB(CURDATE(), INTERVAL 6 DAY)
    </select>

    <!-- Get total sum of all time -->
    <select id="getTotalTotal" resultType="int">
        SELECT COALESCE(SUM(visit_count), 0) FROM visitor_stats
    </select>

    <!-- Get sum of last 48 hours (today + yesterday) -->
    <select id="getRecent48hTotal" resultType="int">
        SELECT COALESCE(SUM(visit_count), 0)
        FROM visitor_stats
        WHERE visit_date >= DATE_SUB(CURDATE(), INTERVAL 1 DAY)
    </select>

</mapper>
